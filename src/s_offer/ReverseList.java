package s_offer;/** * Created by mingkunyu on 2019-11-13 * 输入一个链表，反转链表后，输出新链表的表头。 * * 思路，设置虚拟头节点就是从第二个节点开始每次把它插到头节点之后，直到最后一个节点插完。 */public class ReverseList {    public class ListNode {        int val;        ListNode next = null;        ListNode(int val) {            this.val = val;        }    }    public ListNode ReverseList(ListNode head) {//1-2-3        if (head == null) return null;        ListNode vHead = new ListNode(0);//虚拟头节点        vHead.next = head;//vhead-1-2-3        while (head.next != null) {            //删除2节点            ListNode node=head.next;            head.next=head.next.next;            //插入2节点 vhead-2-1-3            node.next=vHead.next;            vHead.next=node;        }        return vHead.next;    }}